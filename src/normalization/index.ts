/**
 * normalization/index.ts — Normalization module exports
 *
 * CSV Phase 2: Export CSV normalization function.
 * Apify normalization remains in schemas/index.ts (unchanged).
 */

export { normalizeCsvRecords } from './csv';
export type { CsvNormalizationResult } from './csv';
